# GitVersion.yml
next-version: 1.0.0
mode: ContinuousDeployment
tag-prefix: '[vV]'
branches:
  main:
    increment: Patch  # Nur die letzte Stelle der Version erhöhen
    prevent-increment-of-merged-branch-version: true
    tag: ''
